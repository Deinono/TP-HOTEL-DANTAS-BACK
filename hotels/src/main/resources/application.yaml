spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/tp_hotels
    username: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dilect: org.hibernate.dialect.MariaDB103Dialect
